DATABASE_URL=

# Auth.js — GitHub OAuth App (for user login). Create at
# https://github.com/settings/developers -> "New OAuth App".
# Callback URL: http://localhost:3000/api/auth/callback/github
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# GitHub App (separate from the OAuth App above) — installed on repos for
# issue/PR webhook sync. Create at
# https://github.com/settings/apps -> "New GitHub App".
GITHUB_APP_SLUG=
GITHUB_WEBHOOK_SECRET=
